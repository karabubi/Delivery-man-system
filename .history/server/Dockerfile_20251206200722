# server/Dockerfile

# 1. Use a lightweight Node.js image
FROM node:20-alpine

# 2. Create and use /app as the working directory in the container
WORKDIR /app

# 3. Copy package.json and package-lock.json and install dependencies
COPY package*.json ./
RUN npm install

# 4. Copy the rest of your backend source code
COPY . .

# 5. Expose the port your Express server uses
EXPOSE 5000

# 6. Start your server (change server.js if your main file is different)
CMD ["node", "server.js"]
